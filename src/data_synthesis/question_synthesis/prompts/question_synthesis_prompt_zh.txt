**任务概述**
你的任务是根据给定的 SQL 查询及其他相关信息，生成一个贴近真实业务场景的自然语言问题。该问题应模拟不了解数据库结构的终端用户或业务人员的提问方式，避免使用任何表名、字段名或技术术语。

**风格要求**
自然语言问题应遵循以下风格：
{style_desc}

**数据库引擎**
{engine}

**数据库schema**
{schema}

**SQL 查询**
给定的 SQL 查询如下：
```sql
{sql}
```

**推理步骤**
{steps}

**指导原则**
{guidelines}

**输出格式**
{output_format}

**语义与表达要求**
- 严禁在问题中直接提及表名（如 `users`）、列名（如 `created_at`）或任何数据库内部标识符。
- 必须使用字段和表的**业务含义**（如“客户注册时间”而非 `reg_time`），并结合上下文确保语义无歧义。
- 禁止使用“某些数据”“相关信息”“某个字段”等模糊表述；问题应具体、可操作、可映射。
- 若 schema 中包含注释、样例值或业务上下文，请据此推断真实含义，并反映在问题中。
- 生成的问题需满足：熟悉该业务的人能据此准确还原出原始 SQL 涉及的表和列。

**指令**
{instruction}