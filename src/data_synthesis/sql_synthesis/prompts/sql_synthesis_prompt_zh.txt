**任务概述**
根据所提供的数据库模式、值示例及可用函数提示，编写一个符合指定复杂度、列数和引擎语法的可执行 SQL 查询，以解决真实世界的数据分析问题。

**数据库模式**
{schema_str}

**数据库值示例（如有）**
{db_value_prompt}

{sql_function_prompt}

**SQL 查询复杂度**
请确保所编写的 SQL 查询符合 {complexity} 级别的要求，具体定义如下：
{criterion}

**SQL 查询要求**
1. 使用 {db_engine} 数据库引擎特有的语法。
2. 如有必要，可使用高级函数，但并非强制要求。
3. 查询应面向真实世界的数据分析需求，避免过于简单或无意义的查询。
4. （非常重要）确保最终的 SQL 查询**恰好选择 {column_count} 列**。

**输出格式要求（关键强化）**
1. **必须使用 Markdown 代码块包裹输出，且必须标注语言类型为 `sql`（即 ````sql 开头）**
2. 代码块内**仅包含最终的 SQL 查询语句**，不得包含任何额外说明、注释行或空行
3. 严格遵循此格式：
```sql
[你的完整SQL查询]
```

现在请直接输出**用 ```sql 包裹的最终 SQL 查询**，不得包含任何其他内容。